// tauri_offline_scripts.js

// import { iframeModal } from "./iframe-modal.js";

// url = "tauri://localhost/ap_c2s_client/index.html"

// if (window.self.location != url && window.top == window.self) {
//     // We're not in an iframe (like the secure chat modal), so we can initialize the iframe modal 
//     window.secure_chat_modal =
//         window.secure_chat_modal || iframeModal(url, "Secure Chat", "Chat");
// }
